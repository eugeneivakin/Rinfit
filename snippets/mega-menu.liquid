{%- comment -%}
----------------------------------------------------------------------------------------------------------------------
MEGA MENU COMPONENT
----------------------------------------------------------------------------------------------------------------------

Render the mega-menu. The mega-menu is a kind of menu optimized for showing a large number of items into several
columns.

********************************************
Supported variables
********************************************

* link: the link from which we need to render the mega-menu. The link must always be a second level link, but it can
        also be empty (no links), which is the case if the merchant only want to show images, for instance.
* block: the block containing all the information about the menu to render
{%- endcomment -%}
{%- assign menu_count = 0 -%}
{%- for i in (1..5) -%}
  {%- assign menu_setting = 'image_' | append: i | append: '_menu' -%}
  {%- if block.settings[menu_setting] != blank -%}
    {%- assign menu_count = menu_count | plus: 1 -%}
  {%- endif -%}
{%- endfor -%}

{%- if menu_count == 1 and block.settings['image_1_menu'] != blank -%}
<style>
  @media(min-width: 1000px){
    li.header__primary-nav-item[data-title="Company"] .promo-menu-main-header ul{
      margin: 0;
    }
    li.header__primary-nav-item[data-title="Company"] {
      position: relative;
    }
    li.header__primary-nav-item[data-title="Company"] .promo-menu-main-header{
      max-width: fit-content;
      min-width: fit-content;
    }
    .logo_center_navigation_below.header li.header__primary-nav-item[data-title="Company"] ul li:last-child a{
      border: none !important;
    }
    li.header__primary-nav-item[data-title="Company"]  .mega-menu{
      padding: 0;
      width: 160px;
      margin-top: 11px;
      margin-left: -36px;
      padding-top: 15px;
      padding-bottom: 20px;
    } 
  }
</style>

<div class="mega-menu {% if block.settings.images_position == 'left' %}mega-menu--reverse{% endif %}" {{ block.shopify_attributes }}>
    <div class="mega-menu__promo special-structure">
      {%- if block.settings.image_1_heading != blank -%}
        <h3 class="h6">{{ block.settings.image_1_heading }}</h3>
      {%- endif -%}

      <nav class="promo-menu promo-menu-main-header">
        <ul>
          {%- for link in linklists[block.settings.image_1_menu].links -%}
            <li>
              <a href="{{ link.url }}">{{ link.title }}</a>
            </li>
          {%- endfor -%}
        </ul>
      </nav>

      {%- if block.settings.image_1_text != blank -%}
        <p class="smallcaps text-xs text-subdued">{{ block.settings.image_1_text }}</p>
      {%- endif -%}
    </div>
  </div>
{% else %}
<div class="mega-menu {% if block.settings.images_position == 'left' %}mega-menu--reverse{% endif %}" {{ block.shopify_attributes }}>
  {%- if link.levels > 0 -%}
    <ul class="mega-menu__linklist unstyled-list">
      {%- for sub_link in link.links -%}
        <li class="v-stack justify-items-start gap-5">
          <a href="{{ sub_link.url }}" class="h6">{{ sub_link.title }}</a>

          {%- if sub_link.links.size > 0 -%}
            <ul class="v-stack gap-2.5 unstyled-list">
              {%- for sub_sub_link in sub_link.links -%}
                <li>
                  <a href="{{ sub_sub_link.url }}" class="link-faded">{{ sub_sub_link.title }}</a>
                </li>
              {%- endfor -%}
            </ul>
          {%- endif -%}
        </li>
      {%- endfor -%}
    </ul>
  {%- endif -%}

  {%- capture mega_menu_content -%}
    {%- render 'mega-menu-images', context: 'menu', block: block -%}
  {%- endcapture -%}

  {%- if mega_menu_content != blank -%}
    <div class="mega-menu__promo">
      {{- mega_menu_content -}}
    </div>
  {%- endif -%}
</div>
{% endif %}